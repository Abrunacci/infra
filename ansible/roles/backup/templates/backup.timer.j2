# Managed by Ansible (roles/backup).
[Unit]
Description=Daily encrypted backup to R2

[Timer]
OnCalendar=*-*-* {{ backup_time }} UTC
# A run missed while the server was off happens as soon as it is back.
Persistent=true

[Install]
WantedBy=timers.target
