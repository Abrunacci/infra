# Managed by Ansible (roles/hardening).
[sshd]
enabled = true
backend = systemd
maxretry = {{ hardening_fail2ban_maxretry }}
findtime = {{ hardening_fail2ban_findtime }}
bantime = {{ hardening_fail2ban_bantime }}
