{#- Managed by Ansible (roles/projects), from projects.yml. Read by Compose as
    raw KEY=value lines: no comments, since a comment line would not be one. -#}
{% for key, value in (item.backend.env | default({})).items() | sort %}
{{ key }}={{ value }}
{% endfor %}
